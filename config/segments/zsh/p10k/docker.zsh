# segment: docker
# description: Shows current Docker context when non-default
# elements: docker_context
typeset -g POWERLEVEL9K_DOCKER_CONTEXT_SHOW_ON_COMMAND='docker|docker-compose|podman|kubectl|helm'
typeset -g POWERLEVEL9K_DOCKER_CONTEXT_FOREGROUND=38
typeset -g POWERLEVEL9K_DOCKER_CONTEXT_VISUAL_IDENTIFIER_EXPANSION='##ICON##'
